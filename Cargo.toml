[workspace]
members = ["engine", "games/supermunchii", "games/zoomies"]
resolver = "2"

# Profiles live in the workspace root (Cargo ignores them in member manifests).
[profile.release]
opt-level = 3
lto = "thin"
